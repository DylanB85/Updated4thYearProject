{% extends "base.html.twig" %}

{% block title %}Blog Entry{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    <link rel="stylesheet" href="{{ asset('css/blogStyle.css') }}"/>
{% endblock %}

{% block body %}
    <div class="container">
        <div class="blog-header">
            <h1 class="blog-title">Blog</h1>
            <p class="lead blog-description">Specific User Entry</p>
        </div>

        <div class="row">
            <div class="col-sm-8 blog-main">
                <div class="blog-post">
                    <h2 class="blog-post-title">{{ blogPost.title }}</h2>
                    <p class="blog-post-meta">
                        {{ blogPost.updatedAt|date('F j, Y') }} by
                            {{ blogPost.member.username }}
                        </a>
                    </p>
                    <h3>Description</h3>
                    <p>{{ blogPost.description|raw }}</p>

                    <h3>Body</h3>
                    <p>{{ blogPost.body|raw }}</p>
                </div>
            </div>
        </div>
    </div>
{% endblock %}